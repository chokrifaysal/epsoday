version: '3.8'
services:
  epsoday:
    build: .
    volumes:
      - ./targets:/app/targets:ro
      - ./findings:/app/findings
    environment:
      - AFL_I_DONT_CARE_ABOUT_MISSING_CRASHES=1
    ports:
      - "8080:8080"
